# Build artifacts
build/
sdkconfig
sdkconfig.old

# Credentials / secrets
main/secrets.h
main/credentials.h
*.pem
*.key
*.crt
*.der
secure_boot_signing_key.pem
flash_encryption_key.bin
.env

# Structured data backing the generated docs/*.md files - not needed by readers
docs/.data/

# Embedded Assistant extension state (also in global gitignore, kept here for clarity)
.embedded-mastery/

# KiCad backups
hardware/**/*-backups/
hardware/**/_autosave-*
