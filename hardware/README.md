# Hardware

KiCad schematic and PCB layout for **1-2-pwm-led-fade** live here.

- `schematic/` — KiCad project (`.kicad_pro`, `.kicad_sch`)
- `pcb/` — KiCad PCB layout (`.kicad_pcb`) and exported Gerbers for fabrication (JLCPCB/PCBWay)

Redraw the breadboard circuit formally here once Stage 4 (Schematic capture) begins. Use standard reference designators (R1, R2, LED1, Q1, ...).
